# 业务规划与程序编排提案

[English](strategic-planning-and-program-orchestration-proposal.md) | [中文](strategic-planning-and-program-orchestration-proposal.zh-CN.md)

## 目的

这份文档提出 `project-assistant` 在当前工作模型之上可能新增的下一层：

- 战略评估
- 程序编排
- 受监督的长期自动交付

它现在还不是 active 的执行真相。  
它是一份需要先讨论、先 review、再决定是否进入 roadmap 的正式提案。

## 为什么需要这一层

`project-assistant` 现在已经把很多执行侧能力做得比较完整：

- 控制面
- 长任务执行线
- 架构监督
- `progress / continue / handoff`
- 文档整改与 Markdown 治理
- 开发日志
- 验证与发布门禁

但它还没有完全覆盖这个更高层的问题：

`项目接下来应该怎么走，以及当人类不持续盯着时，谁来确保多条长期切片仍然沿着正确方向推进？`

这个空缺会在下面几类场景里反复出现：

| 场景 | 为什么现有层还不够 |
| --- | --- |
| 项目不断暴露根因漂移和专项收口需求 | 执行层可以继续推进，但没有明确的战略层决定何时插入治理专项或架构专项 |
| 旧 milestone 已不再符合现实 | 当前层能看见漂移，但还不会正式提出 roadmap 改造建议 |
| 一个项目已经不只是一条 slice 或一个执行器 | 当前 execution board 只能局部收口，缺少跨 workstream 的编排层 |
| 人类更想只管业务方向，不想持续盯交付过程 | 现有流程已经减少 babysitting，但还没有一个更高层的 AI 监督角色长期盯到真正需要业务裁决为止 |

## 建议的角色模型

| 层 | 主要 Owner | 责任 |
| --- | --- | --- |
| 业务方向层 | 人类 | 提需求、定优先级、定方向、做重大取舍 |
| 战略规划层 | AI 战略规划者 | 判断项目后续怎么走、是否要插治理/架构专项、是否要调整 roadmap 结构、项目定位是否要提升 |
| 程序编排层 | AI 程序编排者 | 管多个 workstreams、切片、依赖和执行器的长期推进 |
| 执行层 | AI 交付执行器 | 写代码、改测试、补文档、跑验证、更新控制面 |
| 治理与恢复层 | 当前 `project-assistant` 能力 | 维护当前真相、门禁、进展、开发日志、文档与恢复链路 |

## 核心原则

人类仍然应该负责：

- 业务方向
- 产品定位
- 重大兼容性决定
- 显著的成本 / 时间取舍
- 需求变化本身

AI 系统则应逐步承担：

- 战略评估建议
- 在已批准方向内的长期编排
- 持续推进多个切片
- 只在真正需要人类判断时升级

## 建议中的新里程碑

### M10：战略评估层

| 项目 | 建议含义 |
| --- | --- |
| 目标 | 判断项目后续应该怎么走、是否应插入治理/架构专项、既有 milestone 或项目定位是否需要调整 |
| 产物 | durable 的战略提案，而不是自动修改 roadmap |
| 人类角色 | review 并批准或否决策略变更 |
| 退出条件 | 战略建议变成显式、可审阅、可追溯到 repo 证据的输出 |

### M11：程序编排层

| 项目 | 建议含义 |
| --- | --- |
| 目标 | 不再只管理一条执行线，而是管理多个 workstreams、切片和执行器 |
| 产物 | durable 的 program board，用来表达活跃流、先后顺序、可并行边界和调度检查点 |
| 人类角色 | 批准编排边界与升级规则 |
| 退出条件 | 系统可以在不频繁依赖人工“继续”的前提下，持续推进几条相关 slice |

### M12：受监督的长期自动交付

| 项目 | 建议含义 |
| --- | --- |
| 目标 | 让 AI 交付系统能持续推进长期工作，直到完成或遇到真正的业务裁决点 |
| 产物 | 更长时间运行的受监督执行流，带周期性检查点、自动升级和 durable 恢复真相 |
| 人类角色 | 只在方向变化或取舍需要批准时介入 |
| 退出条件 | 人类主要负责方向与决策，而不是持续输入“继续” |

## 建议新增的 durable 控制面

| 控制面 | 作用 |
| --- | --- |
| `.codex/strategy.md` | 当前战略判断、专项插入建议、roadmap 调整建议、需要人类裁决的点 |
| `.codex/program-board.md` | 活跃 workstreams、编排状态、串并行边界、执行器分工和调度检查点 |

这两者应该和现有控制面并列，而不是替代 `plan / status`。

## 自动化边界

| 系统可以自动做 | 系统必须升级给人类 |
| --- | --- |
| 提出治理或架构专项建议 | 改产品方向 |
| 提出 roadmap 调整建议 | 改外部承诺 |
| 在已批准方向内编排多个切片 | 改兼容性承诺 |
| 在检查点之间持续推进长期工作 | 接受显著的成本 / 时间变化 |
| 在已批准范围内重排技术顺序 | 需求变化不经 review 就直接吸收 |

## 建议的落地顺序

1. 先定义战略评估文档结构和 review 合约
2. 让战略输出必须基于 repo 证据，而不是只给空泛建议
3. 再引入轻量 program board
4. 等 program board 和升级规则稳定后，再允许跨多个切片做更强编排
5. 最后才考虑更长时间的受监督自动推进

## Review 问题

在把这个提案升级成 active 工作前，需要先回答：

1. `project-assistant` 未来仍然只服务单仓库，还是要变成跨仓库 / 多执行器的编排层？
2. 战略评估是否永远只提建议，还是未来在门禁下可以自动改部分 roadmap？
3. “长期自动推进”最多能跑到什么程度，才必须重新获得人类批准？
4. 系统要插入治理或架构专项，最少需要哪些 durable 证据？

## 当前建议

不要立刻激活这一层。

先完成：

- M8 按语言优化内部输出
- M9 压缩 continue / resume 快照

然后再回来决定，这个提案应不应该变成：

- 路线图里的正式里程碑
- 一个实验模式
- 或者一个位于 `project-assistant` 之上的独立 skill / plugin 层
